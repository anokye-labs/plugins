{
  "name": "omanfo-tests",
  "version": "1.0.0",
  "description": "Anokye-System test harness — TypeScript + @github/copilot-sdk + vitest",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:integration": "vitest run integration",
    "test:e2e": "vitest run e2e",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@github/copilot-sdk": "^0.1.23"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.3.0",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
