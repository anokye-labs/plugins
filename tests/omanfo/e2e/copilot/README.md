# Copilot CLI E2E Tests

This directory contains E2E tests that run against the GitHub Copilot CLI.

## Running

Requires the `copilot` CLI to be installed and authenticated.

```powershell
Invoke-Pester -Path . -Output Detailed
```
