# Claude Code CLI E2E Tests

This directory contains E2E tests that run against the Claude Code CLI.

## Running

Requires the `claude` CLI to be installed and authenticated with an Anthropic API key.

```powershell
Invoke-Pester -Path . -Output Detailed
```
